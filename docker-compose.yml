version: '3'
services:
  jekyll:
    image: jekyll/jekyll:latest
    command: bash -c "bundle install && bundle exec jekyll serve --host 0.0.0.0"
    ports:
      - 4000:4000
    volumes: 
      - .:/srv/jekyll